spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:numerologydbdev
    userName: sa
    password: passworddev
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  sql:
    init:
    mode: always

application:
  http:
    authentication:
      header-name: X-API-KEY
      api-key: riv2698fHCNIGi2mSRsSS5kbBm2cunh2N35I5wxnR27X8Fi7peOKYrUXHspTMdjp