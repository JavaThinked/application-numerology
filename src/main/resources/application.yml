spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:numerologydb
    userName: sa
    password: p@$$w0rdnum3r0l0g1
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
  sql:
    init:
    mode: always