management:
  endpoints:
    web:
      exposure:
        include: health, info
  info:
    env:
      enabled: true

info:
  application:
    name: Numerology Application
    description: Provide and API for numerology
    version: '@project.version@'